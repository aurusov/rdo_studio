Model_name     = sample

Resource_file  = sample
OprIev_file    = sample
Statistic_file = sample
Results_file   = sample
Trace_file     = sample
Frame_file     = sample
Show_mode      = Animation
Show_rate      = 10000.0

Terminate_if Time_now >= 60*1
//Terminate_if select(детали: nocheck).for_all(детали.операция = 3)
