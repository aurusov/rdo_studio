Библиотека ICU является сторонней для Qt. Используется для работы с юникодом WebKit'а. А без последнего плохо отображаются страницы документации РДО.

0. Можно скачать скомпилированные бинарники с goole-disk'а: qt/icu-50.1.2-bin.7z
Но собраны они под Visual Studio 2008 SP1. Если у вас другая версия компилятора, но этот вариант не подходит.

1. Скачать исходники последней версии. На текущий момент - это icu4c-50_1_2-src
http://site.icu-project.org/

2. Разархивировать, например, в C:\icu

2. Для сборки надо поставить cygwin. См. install.cygwin.txt
3. Теперь консольный квест
3.1 Открываем спец. консоль *с правами администратора*
Пуск\Поиск\Visual Studio 2008 Command Prompt

3.3 Добавляем cygwin в PATH прямо из консоли
set PATH=D:\Downloads\cygwin\bin;%PATH%

3.4 Запускаем консоль cygwin'а
D:\Downloads\cygwin\bin\bash.exe

4. Конфигурирование и компиляция
cd /cygdrive/c/icu/source/
./runConfigureICU Cygwin/MSVC

Если ошибка, то выходим из cygwin'а и запускаем
"C:\Program Files (x86)\Microsoft Visual Studio 9.0\VC\bin\vcvars32.bat"
Повторяем 3.4. Если удачно, то

make
make install

5. После сборки, можно удалить C:\icu, т.к. всё нужное будет в D:\Downloads\cygwin\usr\local\

6. Для доступа к скомпиленным dll нужно или прописать путь в PATH или, скопировать
icudt50.dll
icuin50.dll
icuuc50.dll
в
%(QTDIR)/bin

Именно отсюда их будет брать РДО при компиляции, а не из PATH

Сами dll лежат или в D:\Downloads\cygwin\usr\local\lib или выкачиваются с google-disk'а, см. пункт 0
